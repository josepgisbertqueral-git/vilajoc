# Code Snippets - Movement Data Integration\n\n## Copy-Paste Ready Examples\n\n---\n\n## 1. Basic Real-Time Tracking\n\n```python\nfrom src.backends.mediapipe_backend import MediaPipeBackend\nfrom src.core.body_movement_tracker import BodyMovementTracker\nimport cv2\n\n# Initialize\nestimator = MediaPipeBackend(num_poses=2)\nestimator.initialize()\n\ntracker = BodyMovementTracker(buffer_size=30, fps=30.0, use_3d=True)\n\ncap = cv2.VideoCapture(0)\n\n# Main loop\nwhile True:\n    ret, frame = cap.read()\n    if not ret:\n        break\n    \n    # Get pose\n    pose = estimator.process_frame(frame)\n    if pose:\n        # Track movement\n        movements = tracker.update(pose)\n        \n        # Process each person\n        for person_id, movement in movements.items():\n            print(f\"Person {person_id}:\")\n            print(f\"  Position: ({movement.center_x:.3f}, {movement.center_y:.3f})\")\n            print(f\"  Velocity: {movement.velocity_magnitude:.2f} px/s\")\n            print(f\"  Direction: {movement.direction_angle:.1f}°\")\n    \n    # Display\n    cv2.imshow('Movement Tracking', frame)\n    if cv2.waitKey(1) & 0xFF == ord('q'):\n        break\n\ncap.release()\ncv2.destroyAllWindows()\n```\n\n---\n\n## 2. Export to JSON File\n\n```python\nimport json\nfrom pathlib import Path\n\n# Collect data\nall_frames_data = []\n\nwhile video_playing:\n    pose = estimator.process_frame(frame)\n    movements = tracker.update(pose)\n    \n    # Get JSON-ready format\n    frame_data = tracker.get_movements_json()\n    all_frames_data.append(frame_data)\n\n# Save to file\noutput_file = Path('movement_export.json')\nwith open(output_file, 'w') as f:\n    json.dump(all_frames_data, f, indent=2)\n\nprint(f\"✅ Saved {len(all_frames_data)} frames to {output_file}\")\n```\n\n---\n\n## 3. Network Export (UDP Socket)\n\n```python\nimport socket\nimport json\n\nGAME_HOST = '127.0.0.1'\nGAME_PORT = 5000\n\ndef send_to_game(movement_data):\n    \"\"\"Send movement data to game server via UDP.\"\"\"\n    try:\n        sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\n        json_str = json.dumps(movement_data.to_dict())\n        sock.sendto(json_str.encode(), (GAME_HOST, GAME_PORT))\n        sock.close()\n    except Exception as e:\n        print(f\"Error sending to game: {e}\")\n\n# Main loop\nwhile video_playing:\n    pose = estimator.process_frame(frame)\n    movements = tracker.update(pose)\n    \n    # Send each person to game\n    for person_id, movement in movements.items():\n        send_to_game(movement)\n```\n\n---\n\n## 4. Multi-Person Game State\n\n```python\nimport json\n\ndef create_game_state(movements, game_width=1920, game_height=1080):\n    \"\"\"Create game-ready state from movement data.\"\"\"\n    game_state = {\n        'timestamp_ms': int(time.time() * 1000),\n        'num_players': len(movements),\n        'players': []\n    }\n    \n    for person_id, movement in movements.items():\n        player = {\n            'id': person_id,\n            'position': {\n                'x': (movement.center_x - 0.5) * game_width,\n                'y': movement.center_y * game_height,\n                'z': movement.center_z or 0,\n            },\n            'velocity': {\n                'x': movement.velocity_x,\n                'y': movement.velocity_y,\n                'z': movement.velocity_z or 0,\n                'magnitude': movement.velocity_magnitude,\n            },\n            'direction': {\n                'angle_deg': movement.direction_angle,\n                'x': movement.direction_x,\n                'y': movement.direction_y,\n            },\n        }\n        game_state['players'].append(player)\n    \n    return game_state\n\n# Usage\nwhile video_playing:\n    pose = estimator.process_frame(frame)\n    movements = tracker.update(pose)\n    game_state = create_game_state(movements)\n    \n    # Send to game\n    send_to_game_server(json.dumps(game_state))\n```\n\n---\n\n## 5. Event-Based Triggers\n\n```python\nclass MovementEventDetector:\n    \"\"\"Detect movement events for game triggers.\"\"\"\n    \n    def __init__(self, speed_threshold=50, direction_threshold=45):\n        self.speed_threshold = speed_threshold\n        self.direction_threshold = direction_threshold\n        self.prev_directions = {}\n    \n    def detect_events(self, movements):\n        \"\"\"Detect movement events.\"\"\"\n        events = []\n        \n        for person_id, movement in movements.items():\n            # Fast movement\n            if movement.velocity_magnitude > self.speed_threshold:\n                events.append({\n                    'type': 'fast_move',\n                    'person_id': person_id,\n                    'speed': movement.velocity_magnitude,\n                    'direction': movement.direction_angle,\n                })\n            \n            # Direction change\n            if person_id in self.prev_directions:\n                angle_diff = abs(\n                    movement.direction_angle - self.prev_directions[person_id]\n                )\n                if angle_diff > self.direction_threshold and angle_diff < 180:\n                    events.append({\n                        'type': 'direction_change',\n                        'person_id': person_id,\n                        'old_direction': self.prev_directions[person_id],\n                        'new_direction': movement.direction_angle,\n                    })\n            \n            self.prev_directions[person_id] = movement.direction_angle\n        \n        return events\n\n# Usage\ndetector = MovementEventDetector(speed_threshold=100)\n\nwhile video_playing:\n    pose = estimator.process_frame(frame)\n    movements = tracker.update(pose)\n    events = detector.detect_events(movements)\n    \n    for event in events:\n        print(f\"Event: {event}\")\n        # Trigger game action\n        game.trigger_event(event)\n```\n\n---\n\n## 6. Smooth Movement Filtering\n\n```python\nfrom collections import deque\nimport numpy as np\n\nclass SmoothedMovement:\n    \"\"\"Apply smoothing to movement data.\"\"\"\n    \n    def __init__(self, window_size=5):\n        self.window_size = window_size\n        self.buffers = {}\n    \n    def smooth_movement(self, movement):\n        \"\"\"Apply moving average smoothing.\"\"\"\n        person_id = movement.person_id\n        \n        if person_id not in self.buffers:\n            self.buffers[person_id] = deque(maxlen=self.window_size)\n        \n        self.buffers[person_id].append(movement)\n        \n        if len(self.buffers[person_id]) < self.window_size:\n            return movement  # Not enough data\n        \n        # Average velocities\n        velocities = [m.velocity_magnitude for m in self.buffers[person_id]]\n        avg_velocity = np.mean(velocities)\n        \n        # Average direction\n        angles = [m.direction_angle for m in self.buffers[person_id]]\n        avg_direction = np.mean(angles)\n        \n        # Return smoothed version (modified in place)\n        movement.velocity_magnitude = float(avg_velocity)\n        movement.direction_angle = float(avg_direction)\n        \n        return movement\n\n# Usage\nsmoother = SmoothedMovement(window_size=5)\n\nwhile video_playing:\n    pose = estimator.process_frame(frame)\n    movements = tracker.update(pose)\n    \n    for person_id, movement in movements.items():\n        smooth_mvt = smoother.smooth_movement(movement)\n        print(f\"Smoothed speed: {smooth_mvt.velocity_magnitude}\")\n```\n\n---\n\n## 7. Logging Movement Data to File\n\n```python\nimport csv\nfrom datetime import datetime\n\ndef setup_movement_log(filename='movement_log.csv'):\n    \"\"\"Setup CSV logging for movement data.\"\"\"\n    f = open(filename, 'w', newline='')\n    writer = csv.DictWriter(f, fieldnames=[\n        'timestamp', 'person_id', 'center_x', 'center_y', 'center_z',\n        'velocity_x', 'velocity_y', 'velocity_z', 'velocity_magnitude',\n        'direction_angle', 'direction_x', 'direction_y'\n    ])\n    writer.writeheader()\n    return f, writer\n\n# Usage\nlog_file, log_writer = setup_movement_log('my_movements.csv')\n\nwhile video_playing:\n    pose = estimator.process_frame(frame)\n    movements = tracker.update(pose)\n    \n    for person_id, movement in movements.items():\n        log_writer.writerow({\n            'timestamp': datetime.now().isoformat(),\n            'person_id': person_id,\n            'center_x': movement.center_x,\n            'center_y': movement.center_y,\n            'center_z': movement.center_z,\n            'velocity_x': movement.velocity_x,\n            'velocity_y': movement.velocity_y,\n            'velocity_z': movement.velocity_z,\n            'velocity_magnitude': movement.velocity_magnitude,\n            'direction_angle': movement.direction_angle,\n            'direction_x': movement.direction_x,\n            'direction_y': movement.direction_y,\n        })\n\nlog_file.close()\n```\n\n---\n\n## 8. Real-Time Display Overlay\n\n```python\nimport cv2\nimport numpy as np\n\ndef draw_movement_info(frame, movements, frame_width, frame_height):\n    \"\"\"Draw movement data as overlay on frame.\"\"\"\n    \n    for person_id, movement in movements.items():\n        # Center position in pixels\n        x = int(movement.center_x * frame_width)\n        y = int(movement.center_y * frame_height)\n        \n        # Draw center point\n        color = (0, 255, 0) if person_id == 0 else (255, 0, 0)\n        cv2.circle(frame, (x, y), 10, color, -1)\n        \n        # Draw velocity vector\n        vx = movement.velocity_x * 10  # Scale for visibility\n        vy = movement.velocity_y * 10\n        end_x = int(x + vx)\n        end_y = int(y + vy)\n        cv2.arrowedLine(frame, (x, y), (end_x, end_y), color, 2)\n        \n        # Draw text info\n        text = f\"P{person_id}: {movement.velocity_magnitude:.1f}px/s {movement.direction_angle:.0f}°\"\n        cv2.putText(frame, text, (x + 20, y - 10),\n                   cv2.FONT_HERSHEY_SIMPLEX, 0.6, color, 2)\n    \n    return frame\n\n# Usage in main loop\nwhile True:\n    ret, frame = cap.read()\n    if not ret:\n        break\n    \n    pose = estimator.process_frame(frame)\n    if pose:\n        movements = tracker.update(pose)\n        frame = draw_movement_info(frame, movements, frame.shape[1], frame.shape[0])\n    \n    cv2.imshow('Movement Visualization', frame)\n    if cv2.waitKey(1) & 0xFF == ord('q'):\n        break\n```\n\n---\n\n## 9. Performance Optimization\n\n```python\nimport time\n\nclass PerformanceMonitor:\n    \"\"\"Monitor performance metrics.\"\"\"\n    \n    def __init__(self):\n        self.frame_times = deque(maxlen=100)\n        self.tracking_times = deque(maxlen=100)\n    \n    def start_frame(self):\n        self.frame_start = time.time()\n    \n    def end_frame(self):\n        elapsed = time.time() - self.frame_start\n        self.frame_times.append(elapsed * 1000)  # ms\n    \n    def start_tracking(self):\n        self.tracking_start = time.time()\n    \n    def end_tracking(self):\n        elapsed = time.time() - self.tracking_start\n        self.tracking_times.append(elapsed * 1000)  # ms\n    \n    def get_stats(self):\n        return {\n            'avg_frame_time_ms': np.mean(self.frame_times),\n            'avg_tracking_time_ms': np.mean(self.tracking_times),\n            'fps': 1000 / np.mean(self.frame_times) if self.frame_times else 0,\n        }\n\n# Usage\nmonitor = PerformanceMonitor()\n\nwhile video_playing:\n    monitor.start_frame()\n    \n    pose = estimator.process_frame(frame)\n    \n    monitor.start_tracking()\n    movements = tracker.update(pose)\n    monitor.end_tracking()\n    \n    monitor.end_frame()\n    \n    # Print stats every 30 frames\n    if frame_count % 30 == 0:\n        stats = monitor.get_stats()\n        print(f\"FPS: {stats['fps']:.1f}, Tracking: {stats['avg_tracking_time_ms']:.1f}ms\")\n```\n\n---\n\n## 10. Complete Integration Example\n\n```python\nimport cv2\nimport json\nimport numpy as np\nfrom src.backends.mediapipe_backend import MediaPipeBackend\nfrom src.core.body_movement_tracker import BodyMovementTracker\nfrom src.visualization.skeleton_renderer import SkeletonRenderer\n\nclass GameMotionController:\n    \"\"\"Complete motion control for game integration.\"\"\"\n    \n    def __init__(self, game_host='127.0.0.1', game_port=5000):\n        self.estimator = MediaPipeBackend(num_poses=2)\n        self.estimator.initialize()\n        \n        self.tracker = BodyMovementTracker(use_3d=True)\n        self.renderer = SkeletonRenderer()\n        \n        self.game_host = game_host\n        self.game_port = game_port\n        \n        self.movement_history = []\n    \n    def process_frame(self, frame):\n        \"\"\"Process frame and return game state.\"\"\"\n        # Detect pose\n        pose = self.estimator.process_frame(frame)\n        if not pose:\n            return None\n        \n        # Track movement\n        movements = self.tracker.update(pose)\n        \n        # Render visualization\n        frame = self.renderer.render(frame, pose)\n        \n        # Create game state\n        game_state = {\n            'timestamp_ms': self.tracker._frame_count * (1000.0 / 30),\n            'num_players': len(movements),\n            'players': [\n                {\n                    'id': pid,\n                    'pos': [m.center_x, m.center_y],\n                    'vel': [m.velocity_x, m.velocity_y],\n                    'speed': m.velocity_magnitude,\n                    'angle': m.direction_angle,\n                }\n                for pid, m in movements.items()\n            ]\n        }\n        \n        self.movement_history.append(game_state)\n        \n        return game_state, frame\n    \n    def save_session(self, filename='motion_session.json'):\n        \"\"\"Save session data.\"\"\"\n        with open(filename, 'w') as f:\n            json.dump(self.movement_history, f, indent=2)\n    \n    def run(self, camera_id=0):\n        \"\"\"Run main loop.\"\"\"\n        cap = cv2.VideoCapture(camera_id)\n        \n        while True:\n            ret, frame = cap.read()\n            if not ret:\n                break\n            \n            result = self.process_frame(frame)\n            if result:\n                game_state, frame = result\n                # Display game state\n                cv2.putText(frame, json.dumps(game_state['num_players']),\n                           (10, 30), cv2.FONT_HERSHEY_SIMPLEX, 1, (0, 255, 0), 2)\n            \n            cv2.imshow('Game Motion Control', frame)\n            if cv2.waitKey(1) & 0xFF == ord('q'):\n                break\n        \n        cap.release()\n        cv2.destroyAllWindows()\n        self.save_session()\n\n# Usage\ncontroller = GameMotionController()\ncontroller.run()\n```\n\n---\n\n**These snippets are ready to copy and use!** 🚀\n"